<!DOCTYPE html>
<html>
  <head>
    <title>HugeHouseRails</title>
    # <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

 <%= yield %>
</body>

  <footer>

  <div>
  <% if current_user %>
  <h3><%= link_to "Make a Reservation", new_reservation_path %></h3>
  <h3><%= link_to "See All Reservations", reservations_path %></h3>
  <h3><%= link_to "See Your Reservations", client_reservations_path(current_user) %></h3>
  <h3><%= link_to "All Vendors", vendors_path %></h3>
  <%= link_to "Logout", logout_path, method: :delete %>
    <% else %>
    <% end %>

  </div>
  </footer>
</html>