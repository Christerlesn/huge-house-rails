<h3>ALL EVENTS IN THE HUGE HOUSE</h3>
<% @events.each do |event| %>
    <ol>
    <li><%= event.title %></li>
    <%= link_to "Details", event_path(event) %>
    </ol>
<% end %>

<%= link_to "All Reservations", reservations_path %>