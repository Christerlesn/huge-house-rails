<h1> List of Reservations at the Huge House </h1>

<% @reservations.each do |r| %>
    <ul>
    <li><h2><%= r.event.title %></h2>
    <%= link_to "Reservation Details", reservation_path(r) %></li>
    </ul>
<% end %>